# Railway service: pipeline health monitoring and /health endpoint.
[build]
builder = "DOCKERFILE"
dockerfilePath = "services/healthchecker/Dockerfile"
watchPatterns = ["services/healthchecker/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
