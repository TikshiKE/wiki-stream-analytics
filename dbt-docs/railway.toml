# Railway service: static dbt documentation (nginx, ~128 MB RAM).
[build]
builder = "DOCKERFILE"
dockerfilePath = "dbt-docs/Dockerfile"
watchPatterns = ["dbt/**", "dbt-docs/**", "sql/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
