server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(html|json|css|js|svg|png|ico)$ {
        expires 1h;
        add_header Cache-Control "public";
    }
}
