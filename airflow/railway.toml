# Railway service: Airflow standalone (dbt_hourly + maintenance_daily DAGs).
[build]
builder = "DOCKERFILE"
dockerfilePath = "airflow/Dockerfile"
watchPatterns = ["airflow/**", "dbt/wiki_analytics/**", "sql/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
